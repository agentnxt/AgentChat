kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ include "agentchat.fullname" $ }}-configenv
data:
  {{- if (index .Values "agentchat-rag-api" "enabled") }}
  RAG_API_URL: http://{{ include "rag.fullname" (index .Subcharts "agentchat-rag-api") | lower }}.{{ .Release.Namespace | lower }}.svc.cluster.local:8000 
  {{- end }}
  {{- if and (not (dig "configEnv" "MEILI_HOST" "" .Values.agentchat)) .Values.meilisearch.enabled }}
  MEILI_HOST: http://{{ include "meilisearch.fullname" .Subcharts.meilisearch }}.{{ .Release.Namespace | lower }}.svc.cluster.local:7700
  {{- end }}
  {{- if and (not (dig "configEnv" "MONGO_URI" "" .Values.agentchat)) .Values.mongodb.enabled }}
  MONGO_URI: mongodb://{{ include "mongodb.service.nameOverride" .Subcharts.mongodb }}.{{ .Release.Namespace | lower }}.svc.cluster.local:27017/AgentChat
  {{- end }}
  {{- if and (not (dig "configEnv" "USE_REDIS" "" .Values.agentchat)) .Values.redis.enabled }}
  USE_REDIS: "true"
  {{- end }}
  {{- if and (not (dig "configEnv" "REDIS_URI" "" .Values.agentchat)) .Values.redis.enabled }}
  REDIS_URI: redis://{{ include "common.names.fullname" .Subcharts.redis }}-master.{{ .Release.Namespace | lower }}.svc.cluster.local:6379
  {{- end }}
  {{- if .Values.agentchat.configEnv }}
  {{- toYaml .Values.agentchat.configEnv | nindent 2 }}
  {{- end }}
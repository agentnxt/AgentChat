{{- if .Values.agentchat.imageVolume.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "agentchat.fullname" $ }}-images
spec:
  accessModes:
    - {{ .Values.agentchat.imageVolume.accessModes }}
{{- if .Values.agentchat.imageVolume.storageClassName }}
  storageClassName: {{ .Values.agentchat.imageVolume.storageClassName }}
{{- end }}          
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.agentchat.imageVolume.size }}
{{- end }}
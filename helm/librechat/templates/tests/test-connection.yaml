apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agentchat.fullname" . }}-test-connection"
  labels:
    {{- include "agentchat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agentchat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
